# Example environment for docker-compose / webhook. Copy to .env and fill in.
# WEBHOOK_SECRET must match the secret configured on the GitHub webhook.
WEBHOOK_SECRET=replace-me-with-your-github-webhook-secret
